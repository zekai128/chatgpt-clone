const InputComponent = () => {
    return (
        <div style={{ position: 'fixed', bottom: 0, width: '100%', backgroundColor: '#f2f2f2' }}>
        {/* Content of the FixedComponent */}
        <h2>This is the fixed component at the bottom</h2>
        </div>
    );
};

export default InputComponent;