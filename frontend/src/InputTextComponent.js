import React from 'react';

const InputTextComponent = ({ text }) => {
  return (
    <div className="input-text-box">
      <div className="text">{text}</div>
    </div>
  );
};

export default InputTextComponent;